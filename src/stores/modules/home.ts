import * as homeApi from '@/services/home'

const homeService: homeApi.HomeService = homeApi.default

const state = {
  search_list: []
}

const actions = {
}

const getters = {
}

const mutations = {
}

export default {
  namespaced: true,
  state,
  actions,
  getters,
  mutations
}
